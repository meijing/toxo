.container
  =render 'header'
    
  = render 'menu'

  .well
    .bs-component
      .row
        .col-sm-3
          = render 'list_products'
        .col-sm-9
          = render 'gallery'

  = render 'pe'