.container
  =render 'header'
    
  = render 'menu'

  .well
    .bs-component
      .row
        .col-sm-3
          %h4 Alguna de nuestras promociones:
          - @promotions.each do |promo|
            .row
              .btn-xs.panel_products_custom
                %h4= link_to promo.description, show_promotion_path(:id => promo.id), :class => 'color_outlet'
              %br
        .col-sm-9
          = render 'gallery'

  = render 'pe'